#include <iostream>

using namespace std;

// Khởi tạo vị trí của thức ăn (fruit):
fruitX = rand() % width;
fruitY = rand() % height;
if (x == fruitX && y == fruitY) {
    score += 10; // Tăng điểm khi ăn thức ăn
    fruitX = rand() % width; // Tạo vị trí mới cho thức ăn
    fruitY = rand() % height;
    nTail++; // Tăng chiều dài của con rắn
}
int main() {
    float a,b,delta,x1,x2;

    cout << "Nhap vao a: ";
    cin >> a;
    cout << "Nhap vao b: ";
    cin >> b;


    return 0;
}